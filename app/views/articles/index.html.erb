<% @tweets.each do |tweet| %>
  <h3><%= tweet %></h3>
  <% end %>

<h2>Beiber Fevers</h2>
<table>
  <tr>
    <th>Title</th>
    <th>Keyword</th>
  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'New Buzzword', new_article_path %>
